<h1 class="center">All Categories</h1>
<% @categories.each do |category| %>
  <p>Category: <%= link_to category.name, category_path(category), :class => "category" %></p>
<% end %>
<div class="cat_job">
</div>
