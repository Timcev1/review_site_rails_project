<h2 class="center">All Categories</h2>
<div class="col-sm-4">
  <% @categories.each do |category| %>
    <p><b>Category:</b> <%= link_to category.name, category_path(category), :class => "category" %></p>
  <% end %>
<div class="cat_job">
</div>
</div>
